const buffer = Buffer.from("Ibrahim El Gibran");

console.info(buffer); // berbentuk angka
console.info(buffer.toString()); // menampilkan array butter ibrahim el gibran

buffer.reverse(); // membuat nama menjali kebalikan
console.info(buffer.toString());
